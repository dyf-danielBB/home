---
import Head from "../components/Head.astro";
import FallbackImage from "../assets/blog-placeholder-1.jpg";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Header from "../components/Header.astro";

interface Props {
  title?: string;
  description?: string;
  image?: ImageMetadata;
}

const {
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  image = FallbackImage,
} = Astro.props;
---

<!doctype html>
<html lang="zh-CN" data-theme="dark">
  <head>
    <script is:inline>
      (() => {
        const root = document.documentElement;
        let theme = "dark";

        try {
          if (localStorage.getItem("dlc-blog-theme") === "light") {
            theme = "light";
          }
        } catch {
          // The deep-sea default remains available when storage is blocked.
        }

        root.dataset.theme = theme;
      })();
    </script>
    <Head title={title} description={description} image={image} />
  </head>
  <body>
    <div class="dlc-shell mx-auto w-full max-w-2xl px-6 py-12 md:py-16">
      <Header />
      <slot />
    </div>
  </body>
</html>
